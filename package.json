{
  "name": "decidim-badges",
  "version": "0.0.1",
  "description": "Decidim Module to allow users define their own badges",
  "private": true,
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "lint": "eslint -c .eslintrc.json --no-error-on-unmatched-pattern --ignore-pattern app/packs/vendor --ext .js app/packs",
    "stylelint": "stylelint app/packs/**/*.scss"
  },
  "dependencies": {
    "@decidim/browserslist-config": "^0.30.1",
    "@decidim/webpacker": "^0.30.1"
  },
  "devDependencies": {
    "@decidim/dev": "^0.30.1",
    "@decidim/eslint-config": "^0.30.1",
    "@decidim/stylelint-config": "^0.30.1"
  },
  "browserslist": ["extends @decidim/browserslist-config"]
}
